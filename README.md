# flask_url_shortener


## Poetry usage
1. Install poetry in global virutal evn

```bash
pip install poetry
```

2. Install requirements from poetry file (`poetry.lock`)

```bash
poetry install --no-root
```

3. Run server

```bash
poetry run python main.py
```

All commands are executing from project folder in powershell